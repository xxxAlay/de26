-- HQ-SRV
```tgs
apt-get update && apt-get install cups cups-pdf -y
sed -i '18s/Listen localhost:631/Listen hq-srv.au-team.irpo:631/g' /etc/cups/cupsd.conf
cupsctl --share-printers --remote-any
systemctl enable --now cups
systemctl restart cups
```

-- HQ-CLI
```
systemctl restart network
lpadmin -p HQ-PDF -E -v ipp://hq-srv.au-team.irpo/printers/Cups-PDF -m everywhere
lpadmin -d HQ-PDF
echo "Test Page" > ~/testprint.txt
lp -d HQ-PDF ~/testprint.txt
```

-- HQ-SRV
```
cp /var/spool/cups/d00001-001 /raid/nfs/Print
chmod 777 /raid/nfs/Print 
```










